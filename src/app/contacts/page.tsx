import FcUnitedContactsPage from '@/components/fc-united/FcUnitedContactsPage';

/** Static FC United contacts mirror — wget `contacts` (map + form disabled). */
export default function ContactsPage() {
  return <FcUnitedContactsPage />;
}
